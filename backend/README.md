1. npm install express dotenv
2. npm install -D typescript ts-node-dev @types/node @types/express @types/dotenv
3. npm install -D eslint prettier eslint-config-prettier eslint-plugin-prettier
4. npm install -D jest ts-jest @types/jest
5. npx tsc --init


implement jest
npx ts-jest config:init
